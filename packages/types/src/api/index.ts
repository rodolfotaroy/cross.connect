// Re-export API request/response Zod schemas and inferred types.
// Usage in API: import { CreateOrderSchema } from '@xc/types/api'
// Usage in web: import { type CreateOrderInput } from '@xc/types/api'

export * from './schemas';
